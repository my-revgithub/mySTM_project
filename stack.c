#include<stdio.h>
#include<stdlib.h>

#include "typedef.h"
#include "stack.h"

(struct stack *)proc_stack_push(struct stack * head, uint8 data)
{
}
void proc_stack(void)
{
	struct stack *stack_head;
	
	
}